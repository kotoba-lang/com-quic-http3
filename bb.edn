{:paths ["src" "test"]
 :tasks {test {:doc "Run the cljc contract + behavioral test under babashka"
               :extra-paths ["test"]
               :requires ([quic-http3.main-test])
               :task (let [{:keys [fail error]} (clojure.test/run-tests 'quic-http3.main-test)]
                       (System/exit (if (pos? (+ fail error)) 1 0)))}}}
